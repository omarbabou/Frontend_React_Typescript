/* eslint-disable import/no-anonymous-default-export */
import countries from './initial-states/countries'
import authentication from './initial-states/authentication'

export default {
    countries,
    authentication
}